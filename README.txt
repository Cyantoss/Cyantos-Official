How to make changes from VSC to github


Run the command git commit -m "Updated (file) file" in your terminal or Git client.

Push the changes to the remote repository using the command git push origin main 